Title: Update README with --evolve flag documentation
Files: README.md
Issue: none

Add a section documenting the new `--evolve` flag (deprecated) and the `evolve` subcommand. Explain that `--evolve` is retained for backward compatibility but will be removed in future, and encourage use of `greatsage evolve` subcommand. Ensure the README reflects the flag’s behavior and deprecation notice.

