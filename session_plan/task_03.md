Title: Document Evolve Flag in README
Files: README.md
Issue: none

Add a brief section to the project's README describing the new `--evolve` flag, its purpose as a placeholder for the self‑evolution pipeline, and how users can invoke it. Include a code example of the command and a note that full functionality is under development.

Steps:
1. Locate the "Usage" or command‑line options section in `README.md`.
2. Add a bullet or subsection for `--evolve` with a short description.
3. Mention that future iterations will implement the full evolve pipeline.
4. Ensure the edit does not exceed modifications to a single file.

No code changes are required; this is documentation only.
