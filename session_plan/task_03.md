Title: Update README with dry‑run flag description for evolve
Files: README.md
Issue: none

Add a short paragraph under the "Evolve" section of `README.md` documenting the newly added `--dry-run` flag for the evolve subcommand. The description should explain that when the flag is set, the command runs only the assessment and planning phases, generating task files but does not execute any tasks, allowing users to preview the planned work without side‑effects.

Ensure the markdown formatting matches the existing style of the README and that the change does not affect any existing tests.
