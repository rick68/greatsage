Title: Update README with new positional prompt usage
Files: README.md
Issue: none

Revise the command‑line usage documentation in `README.md` to show that users can now provide a prompt as a simple positional argument (e.g., `greatsage "Hello"`) in addition to the existing `--prompt` flag. Add a brief example and note that the REPL mode is unchanged. No code changes needed, only documentation.
