Title: Replace placeholder task generation with real planning based on assessment
Files: src/evolve.rs
Issue: none

Update `planning_phase` to remove the current placeholder loop that creates generic "Placeholder Task" files. Instead, after ensuring the `session_plan` directory exists and is not protected, call the existing helper `generate_tasks_from_assessment` passing the assessment string generated by `assessment_phase`. This will parse the assessment output (lines like "Version: X", "Source files: N", "CI last: status") and create up to three markdown task files with titles such as "Address Version", "Address Source files", "Address CI last". Preserve protected‑path checks. Remove the old `for i in 1..=3` placeholder block.
